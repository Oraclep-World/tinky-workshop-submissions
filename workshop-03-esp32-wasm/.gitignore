# Build artifacts — too large for git, regenerated by the toolchains.
# The build-proof.log files ARE committed (they are the graded proof).
platformio/.pio/
wasm/node_modules/
wasm/build/*.wat
.esphome/
